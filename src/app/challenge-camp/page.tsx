import { ChallengeCampClient } from "./client";

export const metadata = {
  title: 'Challenge Camp | Runway Bystrc',
  description: 'Runway Bystrc | Otevřená a moderní církev v Bystrci',
};

export default function ChallengeCamp() {  
  return (
    <ChallengeCampClient />
  );
}